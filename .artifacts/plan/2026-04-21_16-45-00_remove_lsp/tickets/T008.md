## Goal

Remove LSP test cases and mock methods from config loading, repl support, and request threading tests.

## Owner

backend

## Time

20m

## Difficulty

medium

## Files

- `tests/unit/configuration/test_user_config_loading.py`
- `tests/system/cli/test_repl_support.py`
- `tests/unit/ui/test_request_threading.py`

## Context

Tests currently assert LSP config defaults and mock `update_lsp_for_file` callbacks. These must be cleaned up so the test suite stays aligned with the removed feature.

## Verification

```bash
uv run pytest tests/unit/configuration/test_user_config_loading.py tests/system/cli/test_repl_support.py tests/unit/ui/test_request_threading.py -v
```

All must pass.

## Edits

### `tests/unit/configuration/test_user_config_loading.py`

1. In the partial config fixture, remove the `"lsp"` key:
   ```python
   "lsp": {
       "enabled": False,
   },
   ```

2. Remove assertions:
   ```python
   assert loaded_config["settings"]["lsp"]["enabled"] is False
   assert loaded_config["settings"]["lsp"]["timeout"] == 5.0
   ```

### `tests/system/cli/test_repl_support.py`

1. In the mock app fixture, remove:
   ```python
   self.updated_lsp_files: list[str] = []
   ```
   and:
   ```python
   def update_lsp_for_file(self, filepath: str) -> None:
       self.updated_lsp_files.append(filepath)
   ```

2. In `test_tool_result_callback_posts_message_for_completed_file_edit_without_lsp_update`, remove the assertion `assert app.updated_lsp_files == []`. Optionally rename the test to drop the "without_lsp_update" suffix.

### `tests/unit/ui/test_request_threading.py`

1. In the mock app fixture, remove:
   ```python
   self.lsp_updates: list[str] = []
   ```
   and:
   ```python
   def update_lsp_for_file(self, filepath: str) -> None:
       self.lsp_updates.append(filepath)
   ```

2. Delete `test_tool_result_callback_never_calls_update_lsp_for_file_from_request_thread`.

3. Delete `test_on_tool_result_display_updates_lsp_on_ui_thread_for_file_edits`.
