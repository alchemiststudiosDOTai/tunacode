## Goal

Delete the `src/tunacode/tools/lsp/` package and `src/tunacode/core/ui_api/lsp_status.py`.

## Owner

backend

## Time

15m

## Difficulty

easy

## Files

- `src/tunacode/tools/lsp/__init__.py`
- `src/tunacode/tools/lsp/client.py`
- `src/tunacode/tools/lsp/servers.py`
- `src/tunacode/tools/lsp/diagnostics.py`
- `src/tunacode/core/ui_api/lsp_status.py`

## Context

The LSP subsystem is no longer desired. The first step is to delete the source files that implement it. The `tools/lsp` directory contains the LSP client, server configurations, and diagnostic helpers. The `core/ui_api/lsp_status.py` module is a UI-facing adapter that queries `tools.lsp` for server availability.

## Verification

Run:
```bash
find src -path "*lsp*" -type f | grep -v __pycache__ | grep -v models_registry
```

It should return zero results (except for false positives in the JSON registry, which is unrelated).

## Edits

1. Delete directory `src/tunacode/tools/lsp/`
2. Delete file `src/tunacode/core/ui_api/lsp_status.py`
