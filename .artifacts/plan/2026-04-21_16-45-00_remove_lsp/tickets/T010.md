## Goal

Strip all LSP references from `README.md`, `AGENTS.md`, and module documentation.

## Owner

docs

## Time

20m

## Difficulty

easy

## Files

- `README.md`
- `AGENTS.md`
- `docs/modules/tools/tools.md`
- `docs/modules/tools/tools-system.md`
- `docs/modules/tools/hashline-subsystem.md`
- `docs/modules/ui/ui.md`
- `docs/modules/configuration/configuration.md`

## Context

Stale documentation referencing LSP diagnostics, the `lsp/` package, and LSP status in the UI must be removed to stay consistent with the implementation.

## Verification

```bash
grep -ri "lsp\|LSP" README.md AGENTS.md docs/modules/
```

Should return zero matches (ignore false positives like model IDs in `models_registry.json` if present under `docs/`).

## Edits

### `README.md`

1. Remove the bullet:
   ```markdown
   - **LSP diagnostics** - Real-time code errors after file writes
   ```
2. Remove the entire "## LSP Integration" section (around line 156).

### `AGENTS.md`

1. Remove `src/tunacode/tools/lsp/` from the notable sub-packages bullet (around line 56).

### `docs/modules/tools/tools.md`

1. Remove or update the `lsp/` row in the tools directory table.
2. Remove "prepends LSP diagnostics when available" from the `hashline_edit` description.
3. Remove "prepends LSP diagnostics when available" from the `write_file` description.

### `docs/modules/tools/tools-system.md`

1. Remove `lsp/` from the dependency diagram ASCII art.
2. Remove the bullet about LSP diagnostics ("Returns unified diff and updates LSP diagnostics").
3. Remove the `### lsp/` subsection if it exists.

### `docs/modules/tools/hashline-subsystem.md`

1. Remove the bullet "Optional LSP diagnostics".
2. Remove the sentence "Prepends LSP diagnostics when any are available for the edited file".

### `docs/modules/ui/ui.md`

1. Remove LSP server status from the `resource_bar.py` description.
2. Remove the `render_diagnostics` LSP reference.

### `docs/modules/configuration/configuration.md`

1. Remove the mention of nested `lsp` settings from the `defaults.py` description.
