[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "alchemy-llm-py"
version = "0.1.0"
description = "Python bindings for alchemy-llm (OpenAI-compatible streaming)"
requires-python = ">=3.10"

[tool.maturin]
module-name = "alchemy_llm_py"
bindings = "pyo3"
